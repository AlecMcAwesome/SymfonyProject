{% extends 'base.html.twig' %}
{% block body %}
    {% if is_granted('ROLE_ADMIN') %}
    <h1>add user here</h1>

    {{ form_start(admin_newUser) }}
    {{ form_widget(admin_newUser) }}
    <button type="submit" class="btn btn-success">Add User</button>
    {{ form_end(admin_newUser) }}
    {% endif %}
{% endblock %}

